// src/AirQualityDetail.js
import React from 'react';
import { Typography } from '@mui/material';

const GasDetectionDetail = () => {
  return (
    <div style={{ padding: '20px' }}>
      <Typography variant="h4">Gas Detection Detail</Typography>
      {/* Thêm thông tin chi tiết về chất lượng không khí ở đây */}
    </div>
  );
};

export default GasDetectionDetail;
